<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Toko Online Sederhana</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background: #f5f5f5;
    }
    h1 {
      text-align: center;
    }
    .produk-container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }
    .produk {
      background: #fff;
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 15px;
      width: 200px;
      text-align: center;
    }
    .produk button {
      margin-top: 10px;
      background: #28a745;
      color: white;
      border: none;
      padding: 8px;
      cursor: pointer;
      border-radius: 4px;
    }
    .keranjang {
      margin-top: 30px;
      background: white;
      padding: 15px;
      border-radius: 8px;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
    }
  </style>
</head>
<body>
  <h1>Toko Online Ikhsantuy</h1>

  <div class="produk-container" id="produk-list">
    <!-- Produk akan ditambahkan via JavaScript -->
  </div>

  <div class="keranjang">
    <h2>🛒 Keranjang</h2>
    <ul id="keranjang-list"></ul>
  </div>

  <script>
    const produk = [
      { nama: 'Kaos Keren', harga: 100000 },
      { nama: 'Topi Gaul', harga: 50000 },
      { nama: 'Sepatu Sport', harga: 250000 },
    ];

    const keranjang = [];

    const produkList = document.getElementById('produk-list');
    const keranjangList = document.getElementById('keranjang-list');

    function renderProduk() {
      produk.forEach((item, index) => {
        const div = document.createElement('div');
        div.className = 'produk';
        div.innerHTML = `
          <h3>${item.nama}</h3>
          <p>Rp ${item.harga.toLocaleString()}</p>
          <button onclick="tambahKeKeranjang(${index})">Tambah ke Keranjang</button>
        `;
        produkList.appendChild(div);
      });
    }

    function tambahKeKeranjang(index) {
      keranjang.push(produk[index]);
      renderKeranjang();
    }

    function renderKeranjang() {
      keranjangList.innerHTML = '';
      keranjang.forEach(item => {
        const li = document.createElement('li');
        li.textContent = `${item.nama} - Rp ${item.harga.toLocaleString()}`;
        keranjangList.appendChild(li);
      });
    }

    renderProduk();
  </script>
</body>
</html>

